# Overview: Voices

**URL:** https://docs.ultravox.ai/voices

## Description
Voice options available in Ultravox Realtime

## Endpoint
```
/api/account
```

## Request/Response Examples

### Response Example
```json
{let e=localStorage.key(c);if(e?.endsWith(`-${b}`)&&(d=localStorage.getItem(e),null!=d)){localStorage.setItem(a,d),localStorage.setItem(e,d);break}}
```

### Response Example
```json
{console.error(a),document.documentElement.setAttribute(c,"hidden")}
```

### Response Example
```json
{var c;(Array.isArray(a)?a:[a]).forEach(a=>{let c="class"===a,d=c&&f?e.map(a=>f[a]||a):e;c?(i.classList.remove(...d),i.classList.add(f&&f[b]?f[b]:b)):i.setAttribute(a,b)}),c=b,h&&j.includes(c)&&(i.style.colorScheme=c)}
```

## Full Content

```
Voices
```
