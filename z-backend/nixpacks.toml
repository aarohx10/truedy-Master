[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "pip install -r requirements.txt --break-system-packages"
]

[phases.build]
cmds = []

[start]
cmd = "python3 -m uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2 --log-level info --access-log --no-server-header"

